# Heliaxis Blog — env vars (add to .env / Vercel)

# Public site URL (optional; used for AI referer + sitemap)
# NEXT_PUBLIC_SITE_URL=https://heliaxis.co.uk

# Sanity Free project (public site falls back to mock posts if unset)
# NEXT_PUBLIC_SANITY_PROJECT_ID=
# NEXT_PUBLIC_SANITY_DATASET=production
# SANITY_API_TOKEN=   # Editor token with write access

# AI — OpenRouter now; later set AI_BASE_URL to Anthropic/OpenAI and swap key/model
# OPENROUTER_API_KEY=
# AI_API_KEY=              # alias if not using OpenRouter key name
# AI_BASE_URL=https://openrouter.ai/api/v1
# AI_MODEL=openai/gpt-4o-mini

# Optional: protect /api/blog/publish-due (Vercel Cron)
# CRON_SECRET=
